# Prompt Optimization Recommendations History

This file stores the history of prompt engineering recommendations generated by the Token Optimizer.

---

## [Manual Input] - Apr 17, 2026, 12:51 PM UTC

1. Use XML / Structural Tagging: Replace conversational and wordy transitions (e.g., 'The idea is to tell users...') with clean tags like <core_objective> and <ui_components_required>. This vastly reduces input token bloat while giving the LLM a clearer hierarchical understanding of your requirements.
2. Implement Role + Context Framing: Anchor the prompt by starting with an instruction like 'Act as a Senior UX/UI Engineer.' Establish a high-authority persona to immediately align the model's output tone, ensuring you get professional wireframes rather than generic advice.
3. Apply Skeleton / Scaffolded Prompting: To guarantee the output matches your frontend needs, provide a layout template in the prompt (e.g., `[Header] -> [Hero: Token Value Prop] -> [Features Grid] -> [Footer]`). This forces the AI to structure its response exactly how you intend to build the webpage.
4. Utilize Negative Prompting with Constraint Layering: Stack your requirements by priority and add strict negative constraints. For example, explicitly state: 'Do NOT generate HTML/CSS/JS code; ONLY provide the structural UX layout and text copy.' This prevents the model from wasting hundreds of output tokens on unusable code blocks.

---

## [Prompt 2] - Apr 17, 2026, 12:53 PM UTC

1. XML / Structural Tagging: Wrap different parts of the prompt (like <system>, <task>, <requirements>, and <output_format>) in XML tags. This clearly segments context from instructions, helping the model process constraints more reliably.
2. Output Format Specification & Skeleton Prompting: Instead of loose text requests, provide an exact template or structure for the model to fill out. The optimized prompt uses an <output_format> block to enforce the exact six sections needed.
3. Role + Context Framing: Establish a high-authority persona early (e.g., expert UI/UX designer) to anchor the model's tone and vocabulary, ensuring it uses industry-standard terms like information hierarchy and component wireframes automatically.
4. Constraint Layering: Group constraints logically rather than scattering them. Combining visual consistency, interactivity, and mobile responsiveness under a unified <requirements> tag ensures the model does not drop constraints during generation.

---

## [Manual Input] - Apr 17, 2026, 1:30 PM UTC

1. XML / Structural Tagging: Use tags like <safety_rules> to encapsulate the actual rules the prompt refers to, creating strict boundaries between context, instructions, and constraints.
2. Constraint Layering: Stack requirements in exact priority order (e.g., Priority 1: Safety, Priority 2: Task Execution) so the model knows precisely which constraint wins during a conflict.
3. Negative Prompting: Explicitly list what the web automation tool MUST NOT do (e.g., 'DO NOT submit forms without confirmation') rather than only relying on the broad 'prevent unintended negative consequences'.
4. Role + Context Framing: Establish a high-authority persona with stakes by adding context like 'You are operating in a live environment where safety breaches cause data loss', which forces the model to weigh safety more heavily.

---

## [Manual Input] - Apr 17, 2026, 2:38 PM UTC

1. Role + Context Framing: Assign an 'Ad-Tech Integration Strategist' persona. This forces the model to evaluate market dominance strictly from a B2B Marketing Mix Modeling perspective rather than general app popularity.
2. Data Formatting Efficiency: Convert vertical, line-by-line lists into comma-separated strings. This removes dozens of invisible newline tokens and repetitive spacing overhead, yielding massive input token savings.
3. Constraint Layering: Explicitly cap the recommendation limit (e.g., 'top 10 only'). This prevents the model from attempting to categorize or analyze all 60+ items, drastically reducing output token costs and improving relevance.
4. Output Format Specification / Skeleton Prompting: Direct the model to use a specific output template (e.g., providing a markdown table with specific columns) to prevent wordy, unstructured text generation.
5. Negative Prompting: Instruct the model explicitly on what to avoid (e.g., 'Do not provide technical API integration steps') to keep the focus purely on the business strategy and conserve output tokens.

---

## [Manual Input] - Apr 19, 2026, 8:35 AM UTC

1. Role + Context Framing: Assign a specific persona to improve output quality. For example, prepend 'Act as an expert executive presentation designer' so the model uses punchy, high-level business language.
2. Skeleton / Scaffolded Prompting: Provide an exact template to guarantee the structure of your Word document. Supply a format like '[Slide 1 Title] - Bullet 1 - Bullet 2' so the model directly fills in your desired layout.
3. Negative Prompting: Explicitly list what the AI should avoid to prevent fluff. Use phrases like 'Do not write introductory or concluding paragraphs' and 'Do not use blocks of text longer than two sentences.'
4. Constraint Layering: Stack your requirements in priority order to handle complex intent. First constraint: Extract top 5 themes. Second constraint: Map each theme to a single slide. Third constraint: Format as markdown for easy copying to Word.

---

## [Manual Input] - Apr 21, 2026, 1:46 PM UTC

1. XML / Structural Tagging: Replace conversational filler (e.g., 'Follow the steps below to develop') with rigid tags like <guidelines> and <instructions>. This semantic sectioning significantly reduces token overhead while helping the model isolate distinct system components without confusion.
2. Skeleton / Scaffolded Prompting: To guarantee the model addresses all 5 steps in its long generation, provide an explicit output scaffold in your <output_format> tag. Giving the LLM a structural template prevents it from hallucinating formats and ensures complex multi-part questions are fully answered.
3. Persona Anchoring with Stakes: Strengthen the 'HR tech architect' role by explicitly adding stakes to the constraints. Stating something like 'If the UI is too complex, the non-technical HR team will reject the tool' forces the LLM to deeply prioritize usability throughout its architectural and tech stack recommendations.
4. Constraint Layering & Negative Prompting: Enhance the prompt by explicitly listing what the model should avoid using Negative Prompting (e.g., 'Do NOT recommend enterprise-heavy, high-code legacy tools; focus on modern open-source Python libraries'). This narrows the solution space and yields a much more targeted implementation guide.

---

## [Manual Input] - Apr 24, 2026, 6:40 AM UTC

1. XML / Structural Tagging: Use XML tags like <columns> and <anomalies> to clearly delineate task components. This prevents the model from conflating the data structure with the error injection logic.
2. Skeleton / Scaffolded Prompting: Provide a specific template for the output key (e.g., 'Row 4: Negative clicks (-50) in column 6') to ensure the anomaly list is easily cross-referenced with the generated CSV.
3. Negative Prompting: Add an explicit instruction such as 'DO NOT output the CSV in a markdown table format' to ensure the output remains raw, comma-separated text that can be immediately copy-pasted into Google Sheets.
4. Constraint Layering: Order your instructions so that the core dataset rules are established first, followed by the specific constraints for data corruption. This ensures the model builds a valid foundation before injecting the requested errors.

---
